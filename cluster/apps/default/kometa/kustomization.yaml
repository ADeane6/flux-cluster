apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - ./secret.sops.yaml
  - ./cronjob.yaml
configMapGenerator:
  - name: kometa-config
    files:
      - config/config.yml
      - config/metadata.yml
      - config/letterboxd_mi_amor.yml
generatorOptions:
  disableNameSuffixHash: true
