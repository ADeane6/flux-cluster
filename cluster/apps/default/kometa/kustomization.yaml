apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - config-pvc.yaml
  - secret.yaml
  - cronjob.yaml

configMapGenerator:
  - name: kometa-config
    files:
      - config/config.yml
      - config/metadata.yml
      - config/letterboxd_mi_amor.yml
